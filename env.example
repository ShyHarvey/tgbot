# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
BOT_TOKEN=your_bot_token_here

# Notification channel ID (where messages will be forwarded from)
# This should be the channel ID where your bot is an admin
# You can get this by forwarding a message from the channel to @userinfobot
# IMPORTANT: This value is now static and must be set manually
# The bot will no longer automatically change this when added to other channels
NOTIFICATION_CHANNEL_ID=-1001234567890

# Optional: Custom file to store target chat IDs
TARGET_CHATS_FILE=target_chats.json

# Optional: Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Optional: Polling timeout in milliseconds
POLLING_TIMEOUT=30000

# Optional: Maximum number of target chats
MAX_TARGET_CHATS=100

# Optional: Authorized users list (comma-separated user IDs)
# Get your user ID by sending /test to the bot
# Example: AUTHORIZED_USERS=659506887,659506888,659506889
AUTHORIZED_USERS=659506887
